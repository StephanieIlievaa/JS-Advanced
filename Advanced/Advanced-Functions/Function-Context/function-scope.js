// global context ~ global scope - they are not the same thing
let universe = 'DCU'

function heroFight() {
  
let heroName = 'Superman';
console.log(`${heroName} is from ${universe} universe!`);
   // scope defines where the variables live
   // the context is the place we execute the functions - context ===object
}